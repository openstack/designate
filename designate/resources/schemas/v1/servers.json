{
    "id": "servers",

    "$schema": "http://json-schema.org/draft-03/hyper-schema",

    "title": "servers",
    "description": "Servers",
    "additionalProperties": false,

    "properties": {
        "servers": {
            "type": "array",
            "description": "Servers",
            "items": {"$ref": "server#"}
        }
    }
}
