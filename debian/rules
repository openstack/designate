#!/usr/bin/make -f

%:
	dh $@ --with python-virtualenv --preinstall ${PIP_VERSION_PIN:-"pip==7.1.2"} --preinstall ${SETUPTOOLS_VERSION_PIN:-"setuptools==18.2"} --preinstall "-e file:./designate-dupdomain-hook" --no-test
